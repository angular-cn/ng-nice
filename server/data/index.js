var mongoose = require('mongoose'),    config = require('../config'),    schemas = require("./model/schema"),    kits = require("../kits"),    UserData = require("./user"),    PostData = require("./post"),    connectTimes = 0;var connect = function () {    connectTimes++;    if(connectTimes > 5){        return;    }    var options = { server: { socketOptions: { keepAlive: 1 } } }    mongoose.connect(config.mongoServer, options, function (err) {        if (err) {            kits.logger.error(err, "connect mongodb error");        }else{            kits.logger.info("connect mongodb successfully...");        }    });};// Error handlermongoose.connection.on('error', function (err) {    kits.logger.error(err, "connect mongodb error");});// Reconnect when closedmongoose.connection.on('disconnected', function () {    connect();});// Connect mongodbconnect();process.on('SIGINT', function() {    mongoose.connection.close(function () {        kits.logger.info('Mongoose disconnected through app termination');        process.exit(0);    });});module.exports = {    User: new UserData(schemas.User),    Post: new PostData(schemas.Post)};